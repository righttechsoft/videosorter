name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore dependencies
        run: dotnet restore src/VideoSorter/VideoSorter.csproj

      - name: Build
        run: dotnet build src/VideoSorter/VideoSorter.csproj -c Release --no-restore

      - name: Publish
        run: dotnet publish src/VideoSorter/VideoSorter.csproj -c Release -r win-x64 --self-contained -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o publish

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: publish/VideoSorter.exe
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
